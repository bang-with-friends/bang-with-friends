{
  "name": "common",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc && npm run postbuild",
    "postbuild": "pnpm pack",
    "removeAll": "pnpm recursive run removeCommon --if-present",
    "installAll": "pnpm recursive run installCommon --if-present",
    "postpack": "run-s removeAll installAll",
    "test": "jest --coverage",
    "lint": "eslint --ext .js,.ts ./src/",
    "preinstall": "npx only-allow pnpm"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^14.18.5",
    "@typescript-eslint/eslint-plugin": "^3.10.1",
    "@typescript-eslint/parser": "^3.10.1",
    "eslint": "^7.32.0",
    "eslint-config-airbnb-typescript": "^9.0.0",
    "jest": "^26.6.3",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^26.5.6",
    "typescript": "^3.9.10"
  }
}
